var paragraph = document.getElementById("PAR");

function ChangeFont(rule) {
    paragraph.style.fontFamily = rule;
}
function ChangeAlign(rule) {
    paragraph.style.textAlign = rule;
}

function ChangeHeight(rule) {
    paragraph.style.lineHeight = rule;
}

function ChangeLSpace(rule) {
    paragraph.style.letterSpacing = rule;

}

function ChangeIndent(rule) {
    paragraph.style.textIndent = rule;

}

function ChangeTransform(rule) {
    paragraph.style.textTransform = rule;

}

function ChangeDecorate(rule) {
    paragraph.style.textDecorationLine = rule;

}

function ChangeBorder(rule) {
    paragraph.style.borderStyle = rule;
}

function ChangeBorderColor(rule) {
    paragraph.style.borderColor = rule;
}

